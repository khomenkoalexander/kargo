
# Kafka File Relay

Command line file transfer using Apache Kafka as the data transport.

Large files support, no size limit,
minimum broker log usage. Clients use backend server (clients don't need direct Kafka connection).
Package includes buldled kafka, backend, Command Line Client.